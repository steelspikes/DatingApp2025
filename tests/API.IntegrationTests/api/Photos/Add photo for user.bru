meta {
  name: Add photo for user
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/members/photo
  body: multipartForm
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:multipart-form {
  File: 
}

settings {
  encodeUrl: true
  timeout: 0
}
