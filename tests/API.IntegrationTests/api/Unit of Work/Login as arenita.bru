meta {
  name: Login as arenita
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/login?useCookies=true
  body: json
  auth: inherit
}

params:query {
  useCookies: true
}

body:json {
  {
  	"email": "arenita@test.com",
  	"password": "Pa$$w0rd"
  }
}

script:post-response {
  test("Has Set-Cookie header", function () {
      const cookies = res.getHeader('Set-Cookie');
      expect(cookies).to.include('.AspNetCore.Identity.Application=');
  });
}

settings {
  encodeUrl: true
}
