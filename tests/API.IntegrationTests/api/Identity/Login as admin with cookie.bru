meta {
  name: Login as admin with cookie
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/login?useCookies=true
  body: json
  auth: inherit
}

params:query {
  useCookies: true
}

body:json {
  {
  	"email": "admin@test.com",
  	"password": "Pa$$w0rd"
  }
}

settings {
  encodeUrl: true
}
