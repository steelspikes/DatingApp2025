meta {
  name: Get members with bearer token
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/members
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
