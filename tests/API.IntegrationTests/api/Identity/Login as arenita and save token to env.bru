meta {
  name: Login as arenita and save token to env
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/login
  body: json
  auth: inherit
}

body:json {
  {
  	"email": "arenita@test.com",
  	"password": "Pa$$w0rd"
  }
}

script:post-response {
  const user = res.getBody();
  
  test("Has properties", function () {
      expect(user).to.have.property('accessToken');
  });
  
  if (test("Has properties")) {
      bru.setGlobalEnvVar('token', user.accessToken);
  }
}

settings {
  encodeUrl: true
}
